input {
	kafka{
		bootstrap_servers => "${AUTO_TEST_KAFKA_ADDRESS}"
		topics => ["${AUTO_TEST_KAFKA_TOPIC_NAME}"]
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		index => "${AUTO_TEST_ELK_INDEX_PATTERN}"
	}
}